@model PolloVerde.Contexto.tb_sucursal

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>tb_sucursal</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.descripcion_sucursal)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.descripcion_sucursal)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.tb_region.nombre_region)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.tb_region.nombre_region)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
